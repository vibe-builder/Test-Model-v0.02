# Core ML dependencies
torch>=2.6.0
torchvision>=0.21.0
torchaudio>=2.6.0
transformers>=4.57.1
pydantic>=2.9.2
numpy>=1.23.0

# Quantization and acceleration
torchao>=0.14.1  # Dynamic quantization
bitsandbytes>=0.45.0  # Alternative quantization

# Development and testing
pytest>=8.3.3
pytest-cov>=4.1.0
pylint>=3.3.1
black>=24.10.0

# Data and utilities
datasets>=2.14.0
psutil>=5.9.0
safetensors>=0.4.0
peft>=0.6.0

# Optional acceleration (may not be available on all platforms)
# Note: FlashAttention acceleration is automatically used by PyTorch's SDPA
# when the flash-attn package is available on the system
